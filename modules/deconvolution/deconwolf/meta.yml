# This file was auto-generated by `nextflow module spec`.
#
$schema: https://raw.githubusercontent.com/nextflow-io/schemas/refs/heads/main/module/v1/schema.json
name: deconvolution/deconwolf
version: 1.0.0
description: Generate a PSF and deconvolve a 3D TIFF with deconwolf (dw_bw + dw)
license: Apache-2.0
authors:
- Felix Schneider
input:
- - name: meta
    type: map
    description: >-
      Groovy map with image metadata, e.g. `[id: image_id, emission: 525]`.
      Required keys: `emission` (nm, per-image, drives dw_bw's --lambda);
      `dim_z` (z-plane count, from the convert_to_tif manifest, drives
      dw_bw's --nslice); `physical_voxel_size_xy_nm` (lateral pixel size in
      nm, from the manifest's res_x — assumes square pixels, so res_y is
      not used); `physical_voxel_size_z_nm` (from the manifest's res_z).
  - name: image
    type: file
    description: 3D (ZYX) TIFF to deconvolve, e.g. from the convert_to_tif module
  - name: gpu
    type: boolean
    description: >-
      Whether to run dw with --gpu (true) or CPU threads (false).
output:
- - name: meta
    type: map
    description: Groovy map with image metadata, matching the input `meta`
  - type: file
    description: Deconvolved 3D TIFF, named dw_<input filename> by deconwolf
