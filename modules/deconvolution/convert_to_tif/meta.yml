# This file was auto-generated by `nextflow module spec`.
#
$schema: https://raw.githubusercontent.com/nextflow-io/schemas/refs/heads/main/module/v1/schema.json
name: deconvolution/convert_to_tif
version: 1.0.0
description: Split a bioio-readable image into per-timepoint 3D TIFFs for one channel
  and write a deconvolution-input manifest CSV
license: Apache-2.0
authors:
- Felix Schneider
input:
- - name: meta
    type: map
    description: >-
      Groovy map with image metadata, e.g. `[id: image_id, channel_index: 0]`.
      Required key: `channel_index` (int) — the single channel to extract,
      matching this task's emission wavelength; a multi-channel image gets
      one CSV row (and one CONVERT_TO_TIF call) per channel, never all of
      them at once. Optional keys: `scene` (int, default 0), `time_indices`
      (list of int, default all present).
  - name: image
    type: file
    description: Bioio-readable input image (OME-Zarr, TIFF, or any other bioio-supported format)
output:
- - name: meta
    type: map
    description: Groovy map with image metadata, matching the input `meta`
  - type: file
    description: Manifest CSV (res_x,res_y,res_z,dim_z,channel_index,time_index,scene,filepath) listing every 3D TIFF written, one row per time point (single channel) — res_x/res_y/res_z are pixel sizes in nm, dim_z is the z-plane count, channel_index/time_index/scene identify the row
